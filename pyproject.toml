[project]
name = "health-pipeline"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "cryptography>=47.0.0",
    "dbt-postgres>=1.10.0",
    "python-dotenv>=1.2.2",
]
